# LibraryManagement - Spring Core Hands-on

This is a minimal Spring Core example using XML configuration (applicationContext.xml).

Run:

mvn compile
mvn exec:java -Dexec.mainClass="com.cognizant.library.Application"

It prints book list from `BookService` bean managed by Spring.